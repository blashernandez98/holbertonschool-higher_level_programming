#!/usr/bin/python3
def add_tuple(a=(), b=()):
    c = [0, 0]
    for i in range(0, 2)
        if (a is not None):
            if (a[i] is not None):
                c[i] = a[i]
        if (b is not None):
            if (b[i] is not None):
                c[i] += b[i]
    return (tuple(c))
